# 빌드 컨텍스트에서 뺀다. node_modules를 넣으면 호스트(Windows)에서 만든 바이너리가
# 리눅스 이미지로 그대로 넘어가 npm ci가 만든 것을 덮어쓴다.
node_modules
dist
.vite

# 실제 백엔드가 뜬 뒤로는 쓰지 않는 임시 서버다. 이미지에 들어갈 이유가 없다.
local-server

# 문서는 빌드 산출물에 영향을 주지 않는다 — 고칠 때마다 npm ci 캐시가 깨지는 것만 막는다.
*.md
