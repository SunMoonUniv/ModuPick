# macOS
.DS_Store

# 도구 상태 (로컬 전용)
.omc/

# 폐기된 구 스펙 문서 (로컬 보관 전용 · 저장소에 올리지 않음)
# 주의: docs/DECISIONS.md가 legacy/*.md의 줄 번호를 근거로 인용하고 있습니다.
#       이 폴더가 없으면 근거 추적이 불가능하므로 로컬에서 삭제하지 마세요.
legacy/

docs/
.github