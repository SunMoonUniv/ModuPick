# ModuPick — EC2 배포 실행 방법

> AWS EC2 인스턴스를 만들고, 저장소를 클론하고, 스크립트 하나를 실행하면 서비스가 뜬다.
> 인프라를 몰라도 따라 할 수 있도록 순서대로 적었다.

## 무엇이 만들어지나

스크립트를 실행하면 **컨테이너 두 개**가 하나의 그룹으로 뜬다.

| 컨테이너 | 하는 일 | 포트 |
|---------|--------|------|
| modupick-db | MySQL 8.4. 첫 기동에 테이블 6개와 계정 2개가 자동으로 만들어진다 | 서버 내부에서만 3307 |
| modupick-backend | FastAPI + WebSocket 서버 | 외부 공개 8000 |

호스트에는 별도로 **backend/.venv 가상환경**도 만들어진다. 서비스는 컨테이너가 돌리고, 이 가상환경은 테스트·DB 도구를 서버에서 직접 실행할 때 쓴다.

### DB 계정

| 계정 | 아이디 / 비밀번호 | 권한 |
|------|-----------------|------|
| 애플리케이션 | `modupick` / `modupick` | 데이터 조회·입력·수정. 테이블 구조 변경 불가 |
| 관리자 | `root` / **무작위 생성** — `.env`에 적힌다 | 전부 |
| 마이그레이션 | `modupick_migrator` / **무작위 생성** — `.env`에 적힌다 | 테이블 구조 변경. 평소에는 쓰지 않는다 |

애플리케이션 계정만 값이 고정돼 있다. 이 계정은 **테이블 구조를 못 바꾸고 참가자 기록도 못 지우며**, DB 포트가 서버 내부(127.0.0.1)에만 열려 있어 밖에서 붙을 경로가 없다. 구조를 바꿀 수 있는 나머지 둘은 매번 무작위로 만든다.

값을 바꾸려면 `.env`의 해당 줄을 고치고 `./bootstrap.sh`를 다시 실행한다. 스크립트가 DB 쪽 비밀번호까지 맞춘다.

---

## 1단계 — EC2 인스턴스 만들기

AWS 콘솔 → EC2 → **인스턴스 시작**에서 아래대로 고른다.

| 항목 | 값 | 이유 |
|------|-----|------|
| 이름 | ModuPick | 아무거나 좋다 |
| AMI | **Ubuntu Server 24.04 LTS** 또는 **Amazon Linux 2023** | 둘 다 스크립트가 지원한다 |
| 인스턴스 유형 | **t3.small**(2GB) 권장 · t2.micro/t3.micro(1GB)도 가능 | 1GB면 스크립트가 스왑 2GB를 자동으로 만든다. 대신 느리다 |
| 키 페어 | 새로 만들어서 .pem 파일을 내려받는다 | 이 파일이 없으면 서버에 못 들어간다 |
| 스토리지 | **20GB** 이상 | 도커 이미지가 1.5GB쯤 쓴다 |

### 보안 그룹 — 인바운드 규칙

**이 설정을 빠뜨리면 서비스가 떠도 외부에서 접속되지 않는다.**

| 유형 | 포트 | 소스 | 용도 |
|------|------|------|------|
| SSH | 22 | **내 IP** | 서버 접속 |
| 사용자 지정 TCP | **8000** | Anywhere (0.0.0.0/0) | 백엔드 API·WebSocket |

MySQL 포트(3306·3307)는 **열지 않는다.** DB는 서버 내부에서만 접근하도록 되어 있다.

---

## 2단계 — 서버에 접속

내려받은 `.pem` 파일이 있는 폴더에서 실행한다.

```bash
chmod 400 modupick-key.pem
ssh -i modupick-key.pem ubuntu@<퍼블릭-IP>
```

- Ubuntu AMI면 사용자 이름이 `ubuntu`, Amazon Linux면 `ec2-user`다.
- `<퍼블릭-IP>`는 EC2 콘솔의 인스턴스 상세에서 **퍼블릭 IPv4 주소**를 복사한다.

---

## 3단계 — 저장소 클론

```bash
# git이 없으면 설치 (Ubuntu)
sudo apt-get update && sudo apt-get install -y git

# Amazon Linux면
sudo dnf install -y git
```

```bash
git clone https://github.com/SunMoonUniv/ModuPick.git
cd ModuPick
```

> 비공개 저장소라면 GitHub에서 **Personal Access Token**을 만들어
> `git clone https://<토큰>@github.com/SunMoonUniv/ModuPick.git` 형태로 클론한다.

---

## 4단계 — 스크립트 실행

```bash
chmod +x bootstrap.sh
./bootstrap.sh
```

**처음 실행은 10~30분 걸린다.** 인스턴스가 작을수록 오래 걸린다. 중간에 창을 닫지 않는다.

스크립트가 하는 일은 아홉 단계이고, 진행 상황이 화면에 그대로 나온다.

| 단계 | 내용 |
|:----:|------|
| 1 | OS 확인 |
| 2 | 메모리가 작으면 스왑 2GB 생성 |
| 3 | Docker 엔진 + compose 플러그인 설치 |
| 4 | Python 3.14.6 설치 |
| 5 | backend/.venv 생성 + requirements-dev.txt 설치(테스트·devtools 포함) |
| 6 | .env 생성 — 앱 계정은 modupick/modupick, **root 비밀번호만 무작위로 채운다** |
| 7 | DB 컨테이너 기동 + 스키마 자동 적용 + 검증 |
| 8 | 백엔드 이미지 빌드 + 기동 |
| 9 | `/health` 가 응답할 때까지 대기 |

마지막에 이렇게 나오면 성공이다.

```
═══════════════════════════════════════════════════════════
  세팅 완료
═══════════════════════════════════════════════════════════

NAME               STATUS                   PORTS
modupick-backend   Up 10 seconds (healthy)  0.0.0.0:8000->8000/tcp
modupick-db        Up 1 minute (healthy)    127.0.0.1:3307->3306/tcp

  API        http://<퍼블릭-IP>:8000/docs
  헬스체크    http://<퍼블릭-IP>:8000/health
```

> **스크립트는 여러 번 실행해도 안전하다.** 이미 끝난 단계는 건너뛰고, `.env`가 있으면 덮어쓰지 않는다.

---

## 5단계 — 확인

서버 안에서:

```bash
curl http://127.0.0.1:8000/health
# {"ok":true}
```

내 컴퓨터 브라우저에서:

```
http://<퍼블릭-IP>:8000/health      → {"ok":true}
http://<퍼블릭-IP>:8000/docs        → API 문서 화면
```

브라우저에서 안 열리면 **보안 그룹에 8000 포트가 열려 있는지** 먼저 본다. 이게 가장 흔한 원인이다.

---

## 6단계 — 프론트엔드 주소 연결

프론트엔드를 Vercel 등에 올렸다면 그 주소를 백엔드에 알려 줘야 한다. 알려 주지 않으면 브라우저가 CORS로 요청을 막는다.

```bash
nano .env
```

`CORS_ORIGINS` 줄을 프론트엔드 주소로 바꾼다. 쉼표로 여러 개를 넣을 수 있다.

```
CORS_ORIGINS=https://modupick.vercel.app,http://localhost:5173
```

저장(`Ctrl+O` → `Enter` → `Ctrl+X`)한 뒤 백엔드만 다시 띄운다.

```bash
docker compose up -d backend
```

---

## 자주 쓰는 명령

모두 `ModuPick` 폴더 안에서 실행한다.

| 목적 | 명령 |
|------|------|
| 상태 보기 | `docker compose ps` |
| 백엔드 로그 (실시간) | `docker compose logs -f backend` |
| DB 로그 | `docker compose logs database` |
| 백엔드만 재시작 | `docker compose restart backend` |
| 코드 수정 후 반영 | `git pull && docker compose up -d --build backend` |
| 전체 정지 | `docker compose down` |
| 전체 정지 + **DB 삭제** | `docker compose down -v` |
| 전체 다시 기동 | `docker compose up -d` |

> `docker` 명령에 `permission denied`가 나오면 `exit`로 나갔다가 다시 접속한다.
> 스크립트가 계정을 docker 그룹에 넣었는데, 그 변경은 재접속해야 적용된다.
> 급하면 `sudo docker compose ps` 처럼 `sudo`를 붙여도 된다.

### 가상환경 쓰기

서버에서 테스트나 DB 도구를 직접 돌릴 때 쓴다.

```bash
cd ~/ModuPick/backend
source .venv/bin/activate      # 프롬프트 앞에 (.venv)가 붙는다
pytest                          # 테스트 303개
deactivate                      # 나가기
```

> **테스트를 돌리기 전에 백엔드 컨테이너를 멈춰야 한다.** 둘이 같은 DB를 쓰기 때문에
> 켜 둔 채로 돌리면 테스트가 멈춘 것처럼 보인다.
>
> ```bash
> cd ~/ModuPick                       # docker compose는 항상 저장소 루트에서
> docker compose stop backend
> cd backend && .venv/bin/pytest
> cd .. && docker compose start backend
> ```

### DB 들여다보기

애플리케이션 계정은 **아이디도 비밀번호도 `modupick`**이다.

```bash
docker compose exec database mysql -umodupick -pmodupick modupick
```

이 계정은 조회·입력·수정은 되지만 **테이블 구조는 못 바꾼다.** 참가자 행을 직접 지우는 것도 막혀 있다(지우면 그 사람의 투표가 함께 사라지기 때문이다). 구조를 바꿔야 하면 관리자 계정을 쓴다.

```bash
grep MYSQL_ROOT_PASSWORD .env    # 무작위로 만들어진 값을 확인
docker compose exec database mysql -uroot -p modupick
```

```sql
SHOW TABLES;              -- 6개가 나와야 한다
SELECT COUNT(*) FROM rooms;
exit
```

### 스키마 다시 적용

`db_migration/sql`의 DDL을 고쳤을 때.

```bash
docker compose down -v      # DB를 통째로 비운다
docker compose up -d        # 다시 뜨면서 스키마가 새로 적용된다
```

---

## 문제가 생기면

| 증상 | 원인과 해결 |
|------|-----------|
| 브라우저에서 접속이 안 된다 | 보안 그룹 인바운드에 **TCP 8000**이 열려 있는지 확인한다. 가장 흔한 원인이다 |
| `docker: permission denied` | `exit` 후 다시 SSH 접속한다. 또는 명령 앞에 `sudo`를 붙인다 |
| 스크립트가 Python 설치에서 멈춘다 | 메모리 부족일 수 있다. `./bootstrap.sh --skip-venv` 로 다시 실행하면 컨테이너만 띄운다. 서비스 자체는 정상 동작한다 |
| `modupick-backend`가 `Restarting` 반복 | `docker compose logs backend` 로 원인을 본다. 대개 DB 접속 실패다 |
| 스키마가 적용되지 않았습니다 | DB 볼륨은 있는데 스키마가 없는 상태다. `docker compose down -v && docker compose up -d` |
| `.env에 MYSQL_ROOT_PASSWORD가 필요하다` | `.env` 파일이 없다. `./bootstrap.sh`를 다시 실행하거나 `cp .env.example .env` |
| 디스크가 가득 찼다 | `docker system prune -a` 로 안 쓰는 이미지를 지운다 |
| 컨테이너 이름이 이미 사용 중이다 | `docker compose down --remove-orphans` 후 다시 `up -d` |

로그를 통째로 보려면:

```bash
docker compose logs --tail 100
```

---

## 내 컴퓨터(맥·윈도우)에서 쓸 때

EC2가 아니라 로컬에서 돌릴 때는 `bootstrap.sh`를 쓰지 않는다. Docker Desktop만 켜져 있으면 된다.

```bash
cp .env.example .env        # 최초 1회
docker compose up -d        # DB + 백엔드 둘 다
```

백엔드를 `--reload`로 직접 띄우며 개발할 때는 **DB만** 컨테이너로 띄운다.

```bash
docker compose up -d database

cd backend

# 가상환경이 없다면 최초 1회 (Python 3.14.6이 필요하다)
# requirements-dev.txt가 requirements.txt를 포함하고 pytest·flake8·playwright를 더한다.
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt

source .venv/bin/activate
uvicorn app.main:app --reload --ws-ping-interval 20 --ws-ping-timeout 60
```

프론트엔드는 따로 띄운다.

```bash
cd frontend
npm install
npm run dev      # http://localhost:5173
```

---

## 이 배포에 포함되지 않은 것

현실적으로 필요한 최소 구성만 넣었다. 아래는 **일부러 넣지 않았고**, 필요해지면 그때 추가한다.

| 항목 | 지금 상태 | 필요해지는 시점 |
|------|----------|---------------|
| HTTPS·도메인 | 없다. `http://<IP>:8000`으로 접속한다 | 프론트엔드가 https라면 브라우저가 http API 호출을 막는다. 그때 Nginx + Let's Encrypt 또는 ALB가 필요하다 |
| 자동 재배포(CI/CD) | 없다. `git pull` 후 직접 다시 띄운다 | 배포가 잦아지면 |
| 백업 | 없다 | ModuPick은 재기동마다 모든 방을 지우므로 보존할 데이터가 구조적으로 없다. 스키마는 저장소에 있다 |
| 모니터링 | `docker compose logs`가 전부다 | 사용자가 붙기 시작하면 |
| 프론트엔드 배포 | 이 문서 범위 밖이다 | 설계상 Vercel에 올린다 |

---

## 관련 문서

- DB 스키마와 적용 방법 → `db_migration/README.md`
- 백엔드 설계 문서 → `docs/`
- 컨테이너 구성 상세 → `docker-compose.yml`(주석에 설명이 있다)
