[pytest]
testpaths = tests
pythonpath = .
# timeout-method=thread — 소켓 receive가 막히면 signal로는 깨지지 않는다.
# 이 값이 없으면 헬퍼 하나가 잘못돼도 테스트가 멈춘 채로 남는다.
addopts = -q --tb=short --timeout=15 --timeout-method=thread
