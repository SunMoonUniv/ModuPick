-- ═══════════════════════════════════════════════════════════════════════════
-- 0000_database — 데이터베이스 생성
--
-- 정본   docs/06_database/07_migrations_seed.md
-- 대상   MySQL 8.4
--
-- 이 파일만 데이터베이스를 지정하지 않은 채로 실행한다. 나머지 파일은 전부
-- 대상 데이터베이스에 연결된 상태에서 실행하며, 안에 USE 문을 두지 않는다 —
-- 데이터베이스 이름을 DDL에 박으면 로컬·스테이징·운영이 같은 파일을 쓸 수
-- 없게 된다.
--
-- Docker 컨테이너는 MYSQL_DATABASE 환경 변수가 이미 같은 일을 하므로 이
-- 파일이 필요 없다. EC2에서 기존 MySQL에 직접 적용할 때만 쓴다.
--
--   mysql -h HOST -u root -p < 0000_database.sql
--
-- 문자셋·콜레이션은 서버 기본값에 기대지 않고 여기서 못 박는다. 서버 기본이
-- 다르면 VARCHAR 비교 규칙이 달라져 닉네임 유일성 판정이 환경마다 갈린다.
-- ═══════════════════════════════════════════════════════════════════════════

CREATE DATABASE IF NOT EXISTS modupick
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_0900_ai_ci;
